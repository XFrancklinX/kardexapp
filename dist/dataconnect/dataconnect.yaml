specVersion: "v1beta"
serviceId: "dist"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "dist-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
